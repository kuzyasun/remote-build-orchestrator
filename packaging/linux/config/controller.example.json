{
  "_comment": "Reference only — this file is NOT loaded by rbo-controller.js. The Controller reads configuration exclusively from environment variables; every field below is a documented default plus its real env var. Never commit identity keys or agent credentials beside this file.",
  "mcp_host": { "default": "127.0.0.1", "env": "RBO_MCP_HOST" },
  "mcp_port": { "default": 7410, "env": "RBO_MCP_PORT" },
  "agent_plane_port": { "default": 7411, "env": "RBO_AGENT_PORT" },
  "data_dir": { "default": "~/.rbo", "env": "RBO_DATA_DIR" },
  "allowed_project_roots": {
    "default": [],
    "env": "RBO_ALLOWED_PROJECT_ROOTS",
    "format": "comma-separated absolute paths — job_submit rejects any project_root outside this list"
  },
  "allowed_artifact_destinations": {
    "default": [],
    "env": "RBO_ALLOWED_ARTIFACT_DESTINATIONS",
    "format": "comma-separated absolute paths for artifact_materialize destinations"
  },
  "allow_local_fallback": { "default": true, "env": "RBO_ALLOW_LOCAL_FALLBACK" }
}
