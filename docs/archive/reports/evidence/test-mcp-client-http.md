# Streamable HTTP MCP harness evidence

The automated MCP smoke harness verifies submit, wait, incremental logs, artifacts,
materialization, cancellation, and shared schema validation over Streamable HTTP.

This is protocol-harness evidence, not verification of a product AI client user interface.
