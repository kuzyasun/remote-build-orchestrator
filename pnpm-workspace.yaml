packages:
  - apps/*
  - packages/*
ignoredBuiltDependencies:
  - '@biomejs/biome'
  # better-sqlite3@13+ ships platform prebuilds in-package (N-API); skip the
  # package-manager-injected `node-gyp rebuild` so installs don't require a
  # global node-gyp. Recompile only if you need a custom build.
  - better-sqlite3
