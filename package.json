{
  "name": "rbo-monorepo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "packageManager": "pnpm@10.5.2",
  "engines": {
    "node": ">=22.14.0"
  },
  "scripts": {
    "build": "pnpm --filter \"@rbo/shared\" build && pnpm --filter \"@rbo/protocol\" build && pnpm --filter \"@rbo/snapshot\" build && pnpm --filter \"@rbo/executor\" build && pnpm --filter \"@rbo/testing\" build && pnpm --filter \"@rbo/controller\" build && pnpm --filter \"@rbo/agent\" build && pnpm --filter \"@rbo/cli\" build && pnpm --filter \"@rbo/mcp-stdio\" build",
    "test": "vitest run",
    "typecheck": "pnpm -r typecheck",
    "lint": "biome check .",
    "format": "biome check --write .",
    "rust:verify": "cargo fmt --manifest-path native/windows-executor/Cargo.toml -- --check && cargo test --manifest-path native/windows-executor/Cargo.toml",
    "verify": "pnpm run lint && pnpm run build && pnpm run test && pnpm run rust:verify"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/node": "^22.13.4",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
